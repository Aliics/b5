LET name = "Alexander"

IF name == "Alexander" THEN
    LET statement = "is cool"
    IF statement == "is bad" THEN
        PRINT "OBJECTION!"
    END
    PRINT name + " " + statement + "."
ELSE IF name == "Rewi" THEN
    PRINT "AM REWI!"
ELSE
    PRINT "Go away Tom."
END
